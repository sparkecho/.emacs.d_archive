;;; elisp-conf.el --- Configuration of the Development Enviornment of Emacs Lisp
;;; Commentary:
;;; Code:

;; Appearence
(add-hook 'emacs-lisp-mode-hook 'linum-mode)

(provide 'elisp-conf)
;;; elisp-conf.el ends here
